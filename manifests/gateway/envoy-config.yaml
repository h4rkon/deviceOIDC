apiVersion: v1
kind: ConfigMap
metadata:
  name: envoy-config
  namespace: gateway
data:
  envoy.yaml: |
    admin:
      address:
        socket_address:
          address: 0.0.0.0
          port_value: 9901

    static_resources:
      listeners:
      - name: listener_http
        address:
          socket_address:
            address: 0.0.0.0
            port_value: 8080
        filter_chains:
        - filters:
          - name: envoy.filters.network.http_connection_manager
            typed_config:
              "@type": type.googleapis.com/envoy.extensions.filters.network.http_connection_manager.v3.HttpConnectionManager
              stat_prefix: ingress_http
              generate_request_id: true
              normalize_path: true

              tracing:
                client_sampling: { value: 100.0 }
                random_sampling: { value: 100.0 }
                overall_sampling: { value: 100.0 }
                provider:
                  name: envoy.tracers.opentelemetry
                  typed_config:
                    "@type": type.googleapis.com/envoy.extensions.tracers.opentelemetry.v3.OpenTelemetryConfig
                    service_name: "envoy-gateway"
                    grpc_service:
                      envoy_grpc:
                        cluster_name: otel_collector
                      timeout: 2s

              access_log:
              - name: envoy.access_loggers.stdout
                typed_config:
                  "@type": type.googleapis.com/envoy.extensions.access_loggers.stream.v3.StdoutAccessLog
                  log_format:
                    json_format:
                      ts: "%START_TIME%"
                      req_id: "%REQ(X-REQUEST-ID)%"
                      method: "%REQ(:METHOD)%"
                      path: "%REQ(X-ENVOY-ORIGINAL-PATH?:PATH)%"
                      authority: "%REQ(:AUTHORITY)%"
                      status: "%RESPONSE_CODE%"
                      upstream: "%UPSTREAM_CLUSTER%"

              route_config:
                name: local_route
                virtual_hosts:
                
                - name: hello_vhost
                  domains: ["hello.local", "hello.local:*"]
                  routes:
                  - match: { path: "/healthz" }
                    route: { cluster: hello_upstream }
                  - match: { prefix: "/" }
                    route:
                      cluster: hello_upstream
                      timeout: 15s

                - name: keycloak_vhost
                  domains: ["keycloak.local", "keycloak.local:*"]
                  routes:
                  - match: { prefix: "/" }
                    route:
                      cluster: keycloak
                      timeout: 15s

              http_filters:
              - name: envoy.filters.http.jwt_authn
                typed_config:
                  "@type": type.googleapis.com/envoy.extensions.filters.http.jwt_authn.v3.JwtAuthentication
                  providers:
                    keycloak:
                      issuer: "http://keycloak.local/realms/deviceoidc"
                      remote_jwks:
                        http_uri:
                          uri: "http://keycloak.keycloak.svc.cluster.local:8080/realms/deviceoidc/protocol/openid-connect/certs"
                          cluster: keycloak
                          timeout: 2s
                        cache_duration: 600s
                      forward: true
                  rules:
                  - match: { path: "/healthz" }

                  # allow keycloak without JWT
                  - match:
                      prefix: "/"
                      headers:
                      - name: ":authority"
                        string_match:
                          exact: "keycloak.local"

                  # require JWT only for hello
                  - match:
                      prefix: "/"
                      headers:
                      - name: ":authority"
                        string_match:
                          exact: "hello.local"
                    requires:
                      provider_name: keycloak

              - name: envoy.filters.http.router
                typed_config:
                  "@type": type.googleapis.com/envoy.extensions.filters.http.router.v3.Router

      clusters:
      - name: hello_upstream
        type: STRICT_DNS
        connect_timeout: 2s
        lb_policy: ROUND_ROBIN
        load_assignment:
          cluster_name: hello_upstream
          endpoints:
          - lb_endpoints:
            - endpoint:
                address:
                  socket_address:
                    address: hello.hello.svc.cluster.local
                    port_value: 80

      - name: keycloak
        type: STRICT_DNS
        connect_timeout: 2s
        lb_policy: ROUND_ROBIN
        load_assignment:
          cluster_name: keycloak
          endpoints:
          - lb_endpoints:
            - endpoint:
                address:
                  socket_address:
                    address: keycloak.keycloak.svc.cluster.local
                    port_value: 8080

      - name: otel_collector
        type: STRICT_DNS
        connect_timeout: 2s
        lb_policy: ROUND_ROBIN
        http2_protocol_options: {}
        load_assignment:
          cluster_name: otel_collector
          endpoints:
          - lb_endpoints:
            - endpoint:
                address:
                  socket_address:
                    address: otel-collector.observability.svc.cluster.local
                    port_value: 4317
